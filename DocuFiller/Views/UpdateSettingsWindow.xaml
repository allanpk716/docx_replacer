<Window x:Class="DocuFiller.Views.UpdateSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="更新源设置"
        SizeToContent="Height"
        Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 当前源类型 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="当前源类型：" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding SourceTypeDisplay}" FontWeight="Bold" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- 更新源 URL -->
        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <TextBlock Text="更新源 URL：" Margin="0,0,0,5"/>
            <TextBox Text="{Binding UpdateUrl, UpdateSourceTrigger=PropertyChanged}"
                     Height="28" Padding="4,4,4,4"/>
            <TextBlock Text="留空则使用 GitHub Releases" FontSize="11" Foreground="Gray" Margin="0,3,0,0"/>
        </StackPanel>

        <!-- 更新通道 -->
        <StackPanel Grid.Row="2" Margin="0,0,0,20">
            <TextBlock Text="更新通道：" Margin="0,0,0,5"/>
            <ComboBox ItemsSource="{Binding Channels}" SelectedItem="{Binding Channel}"
                      Height="28" Padding="4,4,4,4" />
        </StackPanel>

        <!-- 按钮区域 -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="保存" Width="80" Height="30" Margin="0,0,10,0"
                    Command="{Binding SaveCommand}"/>
            <Button Content="取消" Width="80" Height="30"
                    Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>
</Window>
