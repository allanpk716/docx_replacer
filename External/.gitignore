# ========================================
# Generated files (do not commit)
# ========================================
# update-config.yaml is generated by sync-version.bat
# It contains the current_version field that is auto-synced with git tags
update-config.yaml

# ========================================
# User-specific configuration (do not commit)
# ========================================
# Local overrides for specific machines/developers
update-config.local.yaml

# ========================================
# Files to KEEP in git (commit these)
# ========================================
# Executable files (needed for builds and releases)
#   - update-client.exe (for update checking)
#   - update-publisher.exe (for publishing releases)
#
# Configuration templates
#   - update-config.yaml.template (template for user configuration)
#   - update-client.config.yaml (reference/example)
#
# Documentation files
#   - update-publisher.usage.txt
#   - publish-client.usage.txt (if exists)

